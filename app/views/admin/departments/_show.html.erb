<div id="department_name"
     name="department[name]"
     class="heading editable"
     data-update_value="'department[name]'"><%=@department.name%></div>
Reports to: <a href="#"><%=department_head_name%></a>
<%=javascript_tag("$.fn.editInPlace.defaults.url='departments/#{resource.id}';$('.editable').editInPlace();")%>
